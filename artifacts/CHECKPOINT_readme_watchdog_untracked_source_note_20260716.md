# Checkpoint: README watchdog untracked-source note

Date: 2026-07-16

## Implemented

- Updated README watchdog helper guidance so `--fail-on-dirty` is described as failing on dirty tracked files and source/docs/test/config-like untracked files while excluding ignored local watchdog JSON artifacts.

## Validation

- `git diff --check` was run before commit.

## Not performed

No FFmpeg, ffprobe, ComfyUI, GPU workload, runtime health probe, render, benchmark, queue execution, prompt submission, public generation, or live approval was run or enabled.
